# 📌 Embedding Models

## 1. Definition
An **Embedding Model** converts text, images, or other data into **numerical vectors** (lists of numbers).  
These vectors capture **semantic meaning**, so similar content has vectors close to each other in vector space.

---

## 2. Why Use Embeddings?
- Machines can’t understand raw text; embeddings turn it into math.  
- Enables:
  - **Semantic search** → Find meaning-based matches, not just keywords.
  - **Clustering** → Group related content.
  - **RAG (Retrieval-Augmented Generation)** → Let LLMs use your own data.

---
## 3.Key Parameters
Dimension size → Depends on model (e.g., 1536 for OpenAI Ada)

Model name → Affects accuracy and performance

Similarity metric → Cosine, Euclidean, dot product

